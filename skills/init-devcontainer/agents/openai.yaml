interface:
  display_name: "Initialize Compose Dev Container"
  short_description: "Initialize general or environment-specific Dev Containers"
  default_prompt: "Use $init-devcontainer to initialize a Compose-backed Dev Container for this project's environment."
