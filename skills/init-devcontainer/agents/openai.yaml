interface:
  display_name: "Initialize Compose Dev Container"
  short_description: "Initialize Compose with shared config and cache volumes"
  default_prompt: "Use $init-devcontainer to initialize a Compose-backed Dev Container in this project."
